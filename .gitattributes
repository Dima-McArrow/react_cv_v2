# Normalise all text files to LF in the repository.
# The project was previously committed from Windows with CRLF endings, which
# turned every edit into a whole-file diff.
* text=auto eol=lf

# Binary assets must never be touched.
*.png binary
*.jpg binary
*.webp binary
*.pdf binary
*.svg text eol=lf
